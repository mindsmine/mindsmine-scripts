language: bash

sudo: required

#os:
#  - linux
#  - osx

matrix:
  include:
    - os: linux
      sudo: required
    - os: osx

addons:
  apt:
    packages:
      - yum

script: bash test/test.sh

notifications:
  email:
  - false
